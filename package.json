{
  "name": "evolved-ephemera",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "astro": "^6.0.8"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
