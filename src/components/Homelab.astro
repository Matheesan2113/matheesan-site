<section id="homelab">
  <div class="container">
    <p class="section-label">03 — homelab</p>
    <h2 class="section-title fade-up">What I run <em>at home</em></h2>
    <p class="homelab-intro fade-up">
      My homelab is a production-grade infrastructure running on Proxmox, a 22TB NAS,
      GPU passthrough, full VLAN segmentation, and a dozen self-hosted services — all
      managed behind wildcard SSL on <code>*.matheesan.ca</code>.
      It's part curiosity, part skill-building, part just loving that it works.
    </p>

    <div class="homelab-grid">
      <div class="hl-card fade-up">
        <div class="hl-card-icon">[px]</div>
        <div class="hl-card-title">Proxmox Hypervisor</div>
        <p class="hl-card-body">AMD Ryzen 5 2600X, 62GB RAM. Dual network: 1Gbps + 2.5Gbps direct link to NAS. Multiple VMs with VLAN isolation.</p>
        <div class="hl-card-tags">
          <span class="hl-tag">Proxmox</span>
          <span class="hl-tag">KVM</span>
          <span class="hl-tag">VLAN</span>
          <span class="hl-tag">ZFS</span>
        </div>
      </div>

      <div class="hl-card fade-up">
        <div class="hl-card-icon">[gpu]</div>
        <div class="hl-card-title">GPU Media Server</div>
        <p class="hl-card-body">RTX A2000 12GB via PCIe passthrough. Powers Jellyfin hardware transcoding, Frigate NVR with YOLOv9m object detection, and Immich ML face recognition.</p>
        <div class="hl-card-tags">
          <span class="hl-tag">RTX A2000</span>
          <span class="hl-tag">CUDA</span>
          <span class="hl-tag">Jellyfin</span>
          <span class="hl-tag">Frigate</span>
        </div>
      </div>

      <div class="hl-card fade-up">
        <div class="hl-card-icon">[nas]</div>
        <div class="hl-card-title">22TB NAS</div>
        <p class="hl-card-body">TerraMaster + OpenMediaVault. Multiple logical volumes for media, personal files, VM backups, and ISOs. 2.5Gbps direct link with jumbo frames.</p>
        <div class="hl-card-tags">
          <span class="hl-tag">OMV</span>
          <span class="hl-tag">NFS</span>
          <span class="hl-tag">22TB</span>
          <span class="hl-tag">2.5Gbps</span>
        </div>
      </div>

      <div class="hl-card fade-up">
        <div class="hl-card-icon">[net]</div>
        <div class="hl-card-title">Network Stack</div>
        <p class="hl-card-body">5 VLANs, GL.iNet Flint 3 router, TP-Link managed switches, Tailscale VPN, AdGuard split DNS, NordVPN gateway for media stack.</p>
        <div class="hl-card-tags">
          <span class="hl-tag">VLAN</span>
          <span class="hl-tag">Tailscale</span>
          <span class="hl-tag">AdGuard</span>
          <span class="hl-tag">WireGuard</span>
        </div>
      </div>

      <div class="hl-card fade-up">
        <div class="hl-card-icon">[cam]</div>
        <div class="hl-card-title">5-Camera Security</div>
        <p class="hl-card-body">Reolink cameras on isolated VLAN 10. Frigate NVR with GPU-accelerated detection, license plate recognition on driveway, face recognition, 4TB storage.</p>
        <div class="hl-card-tags">
          <span class="hl-tag">Frigate</span>
          <span class="hl-tag">YOLOv9m</span>
          <span class="hl-tag">LPR</span>
          <span class="hl-tag">Reolink</span>
        </div>
      </div>

      <div class="hl-card fade-up">
        <div class="hl-card-icon">[ha]</div>
        <div class="hl-card-title">Smart Home</div>
        <p class="hl-card-body">Home Assistant + MQTT broker + Frigate integration. Ecobee, Google Home, Kasa switches, Chromecast. All services HTTPS via Nginx Proxy Manager.</p>
        <div class="hl-card-tags">
          <span class="hl-tag">Home Assistant</span>
          <span class="hl-tag">MQTT</span>
          <span class="hl-tag">NPM</span>
          <span class="hl-tag">SSL</span>
        </div>
      </div>
    </div>

    <div class="spec-grid fade-up">
      <div class="spec-row">
        <span class="spec-key">domain</span>
        <span class="spec-val">*.matheesan.ca — wildcard SSL via Let's Encrypt + Route 53</span>
      </div>
      <div class="spec-row">
        <span class="spec-key">remote access</span>
        <span class="spec-val">Tailscale VPN with subnet routing + split DNS</span>
      </div>
      <div class="spec-row">
        <span class="spec-key">secrets</span>
        <span class="spec-val">Vaultwarden self-hosted + Bitwarden CLI automation</span>
      </div>
      <div class="spec-row">
        <span class="spec-key">media stack</span>
        <span class="spec-val">Sonarr · Radarr · Prowlarr · Bazarr · Jellyseerr · Plex · Jellyfin</span>
      </div>
      <div class="spec-row">
        <span class="spec-key">photos</span>
        <span class="spec-val">Immich — GPU-accelerated ML, face &amp; object recognition</span>
      </div>
      <div class="spec-row">
        <span class="spec-key">auth</span>
        <span class="spec-val">Authentik SSO → Homarr dashboard for family &amp; friends</span>
      </div>
    </div>
  </div>
</section>

<style is:global>
  #homelab { background: var(--bg2); }

  .homelab-intro {
    font-family: var(--serif);
    font-size: 1.15rem;
    line-height: 1.85;
    color: var(--text2);
    max-width: 680px;
    margin-bottom: 4rem;
  }

  .homelab-intro code {
    font-family: var(--mono);
    font-size: 0.85em;
    color: var(--accent);
  }

  .homelab-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .hl-card {
    background: var(--bg3);
    border: 1px solid var(--navy);
    padding: 1.8rem;
    border-radius: 2px;
    transition: border-color 0.3s, transform 0.3s;
  }

  .hl-card:hover { border-color: var(--wave1); transform: translateY(-3px); }

  .hl-card-icon {
    font-family: var(--mono);
    font-size: 1.1rem;
    color: var(--accent);
    margin-bottom: 1rem;
    opacity: 0.8;
  }

  .hl-card-title { font-size: 0.92rem; font-weight: 500; color: var(--text1); margin-bottom: 0.5rem; }
  .hl-card-body  { font-size: 0.82rem; line-height: 1.65; color: var(--text2); }

  .hl-card-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin-top: 1rem;
  }

  .hl-tag {
    font-family: var(--mono);
    font-size: 0.58rem;
    color: var(--text3);
    border: 1px solid var(--navy);
    padding: 0.15rem 0.5rem;
  }

  .spec-grid {
    border: 1px solid var(--navy);
    margin-top: 3rem;
  }

  .spec-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    border-bottom: 1px solid var(--navy);
  }

  .spec-row:last-child { border-bottom: none; }

  .spec-key {
    font-family: var(--mono);
    font-size: 0.65rem;
    letter-spacing: 0.1em;
    color: var(--text3);
    padding: 0.9rem 1.2rem;
    border-right: 1px solid var(--navy);
    background: var(--bg3);
  }

  .spec-val {
    font-size: 0.82rem;
    color: var(--text2);
    padding: 0.9rem 1.2rem;
  }

  @media (max-width: 900px) {
    .homelab-grid { grid-template-columns: repeat(2, 1fr); }
    .spec-row     { grid-template-columns: 1fr; }
    .spec-key     { border-right: none; border-bottom: 1px solid var(--navy); }
  }

  @media (max-width: 600px) {
    .homelab-grid { grid-template-columns: 1fr; }
  }
</style>
